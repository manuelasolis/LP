#include <stdio.h>

int main(){
    int valor, soma = 0;

    for(int i = 1; i <= 10; i++){
        printf("Digite um valor: ");
        scanf("%d", &valor);
        soma += valor;
    }

    printf("Soma total: %d\n", soma);
}
