#include <stdio.h>

int main(){
    int soma = 0, contador = 0;

    for(int i = 15; i <= 100; i++){
        soma += i;
        contador++;
    }

    printf("Media: %.2f\n", (float)soma / contador);
}
